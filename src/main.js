/* syncano main */

// Base function.
var syncano = function() {
  // Add functionality here.
  return true;
};


// Version.
syncano.VERSION = '0.0.0';


// Export to the root, which is probably `window`.
root.syncano = syncano;
